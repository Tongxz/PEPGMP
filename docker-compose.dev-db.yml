
# 开发环境数据库配置
# 只包含：PostgreSQL、Redis
# 用于本地开发时提供数据库服务

networks:
  pepgmp-dev-network:
    driver: bridge

volumes:
  postgres_dev_data:
    driver: local
  redis_dev_data:
    driver: local

services:
  # PostgreSQL 数据库 (开发环境)
  database:
    image: postgres:16-alpine
    container_name: pepgmp-postgres-dev
    environment:
      POSTGRES_DB: pepgmp_development
      POSTGRES_USER: pepgmp_dev
      POSTGRES_PASSWORD: pepgmp_dev_password
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
      - ./scripts/migrations:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"
    networks:
      - pepgmp-dev-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pepgmp_dev -d pepgmp_development"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # Redis 缓存 (开发环境)
  redis:
    image: redis:7-alpine
    container_name: pepgmp-redis-dev
    command: >
      redis-server
      --appendonly yes
      --requirepass pepgmp_dev_redis
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_dev_data:/data
    ports:
      - "6379:6379"
    networks:
      - pepgmp-dev-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  # Adminer (数据库管理工具 - 可选，暂时注释)
  # adminer:
  #   image: adminer:latest
  #   container_name: pyt-adminer
  #   ports:
  #     - "8081:8080"
  #   networks:
  #     - pepgmp-dev-network
  #   environment:
  #     ADMINER_DEFAULT_SERVER: database
  #   depends_on:
  #     - database
  #   restart: unless-stopped

