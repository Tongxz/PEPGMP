# 生产环境依赖文件
# 注意：此文件已弃用，请使用 pyproject.toml 管理依赖
# 使用方法：pip install -e .

# 如果需要安装所有依赖，使用：
# pip install -e ".[dev,test,docs,production]"

# 基础依赖（已迁移到 pyproject.toml）
-e .

# 开发依赖（可选）
# pip install -e ".[dev]"

# 生产依赖（可选）
# pip install -e ".[production]"

# 数据库相关依赖（新增）
asyncpg>=0.29.0
databases[postgresql]>=0.8.0
sqlalchemy>=2.0.0

# Redis客户端
redis>=5.0.0
aioredis>=2.0.1
python-dotenv
